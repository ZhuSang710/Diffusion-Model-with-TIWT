* [x] 已新增环境配置文本文件，且内容覆盖：Python 版本、CUDA/驱动前置、Conda 与 venv 两种安装路径

* [x] 已提供可直接安装的依赖清单文件，覆盖代码中第三方 import（torch/torchvision、yaml、skimage、medpy、lmdb、pywt 等）

* [x] 文档包含数据目录结构与示例命令（训练/采样/测试），与 README 与 fast\_ddpm\_main.py/ddpm\_main.py 参数一致

* [x] 文档包含最小导入校验命令，预期不会出现 ModuleNotFoundError

* [x] 文档包含 PyTorch/torchvision 版本组合说明（CPU/GPU），并给出默认推荐组合与替代组合

